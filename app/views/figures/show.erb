<h4>Figure Name <a href="/figures/<%= @figure.id %>"><%= @figure.name %></a></h4>


<a href="/figures/<%= @figure.id %>"><%= @figure.name %></a>



<ul>
  Landmarks:

    <% @figure.landmarks.each do |landmark| %>
      <%= landmark.name %>
    <% end %>
</ul>

<ul>
  Titles:

    <% @figure.titles.each do |title| %>
      <%= title.name %>
    <% end %>
</ul>

  <a class="button" href="/figures">Go Home</a>



<!-- <h1>Figure Name</h1>

  <table>
    <tr>
      <th>Name<a href="/figures/<%= @figure.id %>"><%= @figure.name %></a></th>
    </tr>

    <tr>
      Landmarks:

        <% @figure.landmarks.each do |landmark| %>
          <%= landmark.name %>
          <%= landmark.year_completed %>
        <% end %>
    </tr>

    <tr>
      Titles:

        <% @figure.titles.each do |title| %>
          <%= title.name %>
        <% end %>
      </tr>
  <table> -->
